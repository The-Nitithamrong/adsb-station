# ของลับ + ข้อมูล — อย่า commit
*.env
fr24-watchdog.env
*.db
preview.png
# python
__pycache__/
*.pyc
# ESP32 watchdog config (มี wifi pass + tuya local_key — อย่า commit)
watchdog-esp32/config.py
watchdog-esp32-display/src/config.h
# home assistant / mosquitto runtime (config + secrets — อย่า commit)
deploy/homeassistant/config/
deploy/homeassistant/mosquitto/data/
deploy/homeassistant/mosquitto/log/
deploy/homeassistant/mosquitto/config/passwd

# wrangler cache/logs (Cloudflare Pages deploy) — artifact อย่า commit
.wrangler/
